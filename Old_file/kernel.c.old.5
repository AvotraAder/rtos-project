#include <stdint.h>
#include <stddef.h>
#include "gdt.h"
#include "idt.h"
#include "timer.h"
#include "task.h"

static uint16_t* const VGA_BUFFER = (uint16_t*) 0xB8000;
static const int VGA_WIDTH = 80;
static const uint8_t VGA_COLOR = 0x0F;

static inline uint16_t vga_entry(char c, uint8_t color) {
    return (uint16_t) c | (uint16_t) color << 8;
}

void terminal_write_at(const char* str, int row, int col) {
    for (size_t i = 0; str[i] != '\0'; i++) {
        VGA_BUFFER[row * VGA_WIDTH + col + i] = vga_entry(str[i], VGA_COLOR);
    }
}

void terminal_write_dec(uint32_t n, int row, int col) {
    if (n == 0) {
        VGA_BUFFER[row * VGA_WIDTH + col] = vga_entry('0', VGA_COLOR);
        VGA_BUFFER[row * VGA_WIDTH + col + 1] = vga_entry(' ', VGA_COLOR);
        return;
    }
    char buf[32];
    int i = 0;
    while (n > 0) {
        buf[i++] = '0' + (n % 10);
        n /= 10;
    }
    for (int j = 0; j < i; j++) {
        VGA_BUFFER[row * VGA_WIDTH + col + j] = vga_entry(buf[i - 1 - j], VGA_COLOR);
    }
    VGA_BUFFER[row * VGA_WIDTH + col + i] = vga_entry(' ', VGA_COLOR);
}

void task_a(void) {
    uint32_t count = 0;
    while (1) {
        terminal_write_at("Tache A (Exec en parallele) : ", 6, 0);
        terminal_write_dec(count++, 6, 30);
        for (volatile int i = 0; i < 1000000; i++);
    }
}

void task_b(void) {
    uint32_t count = 0;
    while (1) {
        terminal_write_at("Tache B (Exec en parallele) : ", 7, 0);
        terminal_write_dec(count++, 7, 30);
        for (volatile int i = 0; i < 1000000; i++);
    }
}

void kernel_main(void) {
    init_gdt();
    init_idt();
    init_timer(100);

    init_tasking();
    create_task(task_a);
    create_task(task_b);

    terminal_write_at("--- RTOS Multitache Preemptif Actif ---", 4, 0);

    __asm__ __volatile__("sti");

    while (1) {
        __asm__ __volatile__ ("hlt");
    }
}
